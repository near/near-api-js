image: node:10

variables:
  NODE_ENV: ci

stages:
  - test

#test_master:
#  stage: test
#  tags:
#    - gcloud
#  script:
#    - yarn
#    - yarn test

test_staging:
  stage: test
  tags:
    - gcloud
  script:
    - yarn
    - NODE_ENV=ci-staging yarn test

lint:
  stage: test
  tags:
    - gcloud
  script:
    - yarn
    - yarn lint
